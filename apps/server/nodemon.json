{
  "watch": ["src", "drizzle"],
  "ext": "ts,sql,json",
  "ignore": ["src/**/*.test.ts"],
  "exec": "npm run db:migrate && ts-node --require tsconfig-paths/register src/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
