{
  "version": 2,
  "name": "civjs-server",
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm ci",
  "ignoreCommand": "bash -c 'git diff --quiet origin/master...HEAD -- apps/server/ package.json package-lock.json turbo.json \"*/vercel.json\"'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}