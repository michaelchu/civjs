{
  "version": 2,
  "name": "civjs-server",
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm ci",
  "ignoreCommand": "bash -c '! git diff HEAD^ HEAD --name-only | grep -qE \"^(apps/server/|package\\.json|package-lock\\.json|turbo\\.json|.*/vercel\\.json)\"'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}