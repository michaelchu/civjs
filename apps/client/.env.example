# Local development server URL
VITE_SERVER_URL=http://localhost:3001

# Tileset variant toggle (ENV-only). Default is "new" if unset.
# Options: legacy | new
# - legacy uses /js/2dcanvas and /tilesets
# - new uses /js/2dcanvas/new and /tilesets/new
VITE_TILESET_VARIANT=new
