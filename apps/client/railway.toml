[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "always"

# Use Vite build and serve for production
startCommand = "npm run build && npx vite preview --host 0.0.0.0 --port $PORT"