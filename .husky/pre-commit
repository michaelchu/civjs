# Run type checking, format checking (strict), and linting (errors only)
# Allow existing 119 warnings but catch any new errors
npm run typecheck && npm run format:check && npm run lint -- --max-warnings=119